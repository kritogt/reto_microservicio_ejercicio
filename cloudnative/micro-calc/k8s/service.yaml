apiVersion: v1
kind: Service
metadata:
  name: micro-calc-service
  namespace: calculadora
spec:
  type: NodePort
  selector:
    app: micro-calc
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080